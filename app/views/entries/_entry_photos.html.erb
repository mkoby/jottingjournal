<% unless @entry.entry_photo.new_record? %>
  <%= link_to image_tag(@entry.photo.photo.thumb260.url, :class => "img-polaroid"), 
    @entry.photo.photo.url, 
    :class => "pull-right", 
    :style=> "padding-left: 5px; padding-bottom: 5px;" %>
<% end %>


<!DOCTYPE html>
<html>
<head>
  <title>Jot Journal - Your Life, Your Journal</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="navbar">
      <div class="navbar-inner">
        <%= link_to 'Jot Journal', root_path, :class => "brand" %>
        <ul class="nav pull-right">
          <% if current_user %>
            <li><%= link_to 'New Entry', new_entry_path %></li>
          <% end %>
          <li><%= link_to(current_user ? 'Logout' : 'Login', new_user_session_path) %></li>
        </ul>
      </div>
    </div>

    <p>
    <% flash.each do |key, msg| %>
      <%= display_flash(key, msg) %>
    <% end %>
    </p>

    <%= yield %>

  </div>

</body>
</html>
